#include <stdio.h>
int main (){
    int N, i, NUM;
    printf("\nSu quanti bit deve essere effettuata la conversione?");
    scanf("%d", &N);
    int binario [N];
    for (i=0, i<N, i++){
        printf("Inserire la %d cifra", i);
        scanf ("%d", &NUM);
    }
    printf("Inserire un numero in binario");
    
}